const entries = (state) => {
  return state.entries
}
const getEntriesByTerm = (/* state */) => {}
const getEntryById = (/* state */) => {}

export default { entries, getEntriesByTerm, getEntryById }
